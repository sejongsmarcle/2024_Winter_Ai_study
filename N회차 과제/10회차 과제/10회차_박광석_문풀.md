# 현희섭
```python
# true = np.ones((batch_size, 1))
true = np.full((batch_size, 1), 1)
# fake = np.zeros((batch_size, 1))
fake = np.full((batch_size, 1), 0)
```
```python
from tensorflow.keras.datasets import mnist
from tensorflow.keras.layers import Input, Dense, Reshape,Flatten, Dropout
from tensorflow.keras.layers import BatchNormalization, Activation, LeakyReLU, UpSampling2D, Conv2D, Conv2DTranspose
from tensorflow.keras.models import Sequential, Model

import numpy as np
import matplotlib.pyplot as plt
import os

# 생성자 모델
generator = Sequential()
generator.add(Dense(128*7*7, input_dim=100, activation=LeakyReLU(0.2)))
generator.add(BatchNormalization())
generator.add(Reshape((7, 7, 128)))
generator.add(Conv2DTranspose(filters=64, kernel_size=5, padding='same'))
generator.add(BatchNormalization())
generator.add(Activation(LeakyReLU(0.2)))
generator.add(Conv2DTranspose(filters=1, kernel_size=5, padding='same', activation='tanh'))
```
colab에서 2번 코드를 돌려봤는데 오류가 안나서 답인지는 모르겠네요... 사이트 보고 했는데
# 권효정
1. 생성자
2. 판별자
3. 적대적
4. 0
5. 1
6. 비지도
